import { useUserContext } from '../../context/userContext';
import styles from './card.module.css';


export default function Card() {
    return(
        <nav className={styles['card-container']}>
            
        </nav>
    )

}